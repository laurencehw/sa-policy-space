# Archived SQL migrations

One-off SQL scripts that have already been applied to both SQLite (local) and
Supabase (production). Kept for reference only — do not re-run.

Includes:
- `supabase_*.sql` — Supabase-specific migrations (housing, health, higher ed batches)
- `enrichment_*.sql` — Policy idea description enrichment batches
- `sqlite_*.sql` — SQLite-specific one-off migrations
